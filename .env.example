# ========================================
# LocalLend Environment Variables Template
# ========================================
# Copy this file to:
# - backend/.env (for backend configuration)
# - frontend/.env (for frontend configuration)

# ========================================
# BACKEND ENVIRONMENT VARIABLES
# Place in: backend/.env
# ========================================

# MongoDB Configuration
MONGODB_URI=mongodb://mongodb:27017/locallend

# JWT Configuration (CHANGE IN PRODUCTION!)
JWT_SECRET=your-secure-jwt-secret-here-change-this-in-production
JWT_EXPIRATION_MS=86400000

# Cloudinary Configuration (Image Storage)
# Get these from https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Server Configuration
SERVER_PORT=8080
APP_NAME=locallend

# ========================================
# FRONTEND ENVIRONMENT VARIABLES
# Place in: frontend/.env
# ========================================

# API Configuration
VITE_API_BASE_URL=http://localhost:8080

# App Configuration
VITE_APP_TITLE=LocalLend
VITE_NODE_ENV=development

# ========================================
# NOTES
# ========================================
# 1. Never commit actual .env files to git
# 2. Generate a secure JWT_SECRET for production:
#    - Use: openssl rand -base64 64
#    - Or: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
# 3. For Docker: MongoDB hostname is 'mongodb' (service name)
# 4. For local development: MongoDB hostname is 'localhost'
# 5. Cloudinary is required for image upload functionality
